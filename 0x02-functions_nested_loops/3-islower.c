#include "main.h"
#include <stdlib.h>
#include <stdio.h>
/**
 * print_alphabet_x10 - void function
 * islower
 * Return: Always 0.
 */
int _islower(int c)
{
   if (c >= 97 && c <= 122)
	{
		return (1);
	}
    	else 
        	return (0);
}
